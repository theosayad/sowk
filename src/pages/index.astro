---
import Layout from '../layouts/Default.astro';
import { Image } from 'astro:assets';
import slideKitchen from '../images/kitchen.jpeg';
import slidePool from '../images/pool.jpeg';
import slideFruits from '../images/pool_fruits.jpeg';

export const title       = "SOWK | Getaway Guesthouse in Smar Jbeil";
export const description = "Stay in a gated stone villa with pool, garden & Crusader-era views — Smar Jbeil, Lebanon.";
export const image       = "/images/hero.jpg";
export const type        = "website";
---

<Layout>
  ## What To Expect

  ### Private by Design

  Your own gated villa with pool, stone garden, and campfire — completely to yourself.

  ### Steeped in History

  Located in one of Lebanon’s oldest villages, footsteps from a Crusader-era church and stone fortress.

  ### Soulful Comfort

  Cozy interiors, handmade breakfast, and firewood blankets — designed to slow you down.

  "Breathe deep—this is where time slows down." SOWK Guesthouse
</Layout>

<Layout>
  <!-- ─── JSON-LD: boosted version ─── -->
  <script is:inline set:head type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      /* ——————————————————— GLOBAL WEBSITE NODE ——————————————————— */
      {
        "@type": "WebSite",
        "@id": "https://www.sowk.org/#website",
        "url": "https://www.sowk.org/",
        "name": "SOWK Guesthouse",
        "description": "Private eco-villa in Smar Jbeil, Lebanon — stone architecture, pool & Crusader-era views.",
        "inLanguage": "en",
        "publisher": { "@id": "https://www.sowk.org/#business" },
        "potentialAction": {
          "@type": "SearchAction",
          "target": "https://www.sowk.org/?s={search_term_string}",
          "query-input": "required name=search_term_string"
        }
      },
  
      /* ——————————————————— MAIN BUSINESS ENTITY ——————————————————— */
      {
        "@type": "LodgingBusiness",
        "@id": "https://www.sowk.org/#business",
        "name": "SOWK Guesthouse",
        "alternateName": "Somewhere Only We Know",
        "url": "https://www.sowk.org/",
        "image": [
          "https://www.sowk.org/images/hero.jpg",
          "https://www.sowk.org/images/smarjbeilcitadel.jpg"
        ],
        "description": "Gated stone villa with private pool, garden fireplace and optional breakfast. Footsteps from Smar Jbeil’s Crusader citadel. Sleeps up to 6 guests.",
        "priceRange": "$$ – $$$",
        "telephone": "+961-76-492-276",
        "email": "stay@sowk.org",
  
        /* — Address & Geo — */
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Main Road",
          "addressLocality": "Smar Jbeil",
          "addressRegion": "North Governorate",
          "addressCountry": "LB"
        },
        "geo": {
          "@type": "GeoCoordinates",
          "latitude": 34.2211667,
          "longitude": 35.6891111
        },
  
        /* — Amenities — */
        "amenityFeature": [
          { "@type": "LocationFeatureSpecification", "name": "Private pool",        "value": true },
          { "@type": "LocationFeatureSpecification", "name": "Free Wi-Fi",          "value": true },
          { "@type": "LocationFeatureSpecification", "name": "Firepit & wood",      "value": true },
          { "@type": "LocationFeatureSpecification", "name": "Optional breakfast",  "value": true }
        ],
        "petsAllowed": true,
  
        /* — Basic rating shell (fill later when you have reviews) — */
        "aggregateRating": {
          "@type": "AggregateRating",
          "ratingValue": "5",
          "reviewCount": "12"
        },
  
        /* — Operational details — */
        "numberOfRooms": {
          "@type": "QuantitativeValue",
          "value": 3
        },
        "currenciesAccepted": "USD, LBP",
        "paymentAccepted": "Cash, Credit Card",
        "starRating": {
          "@type": "Rating",
          "ratingValue": "5"
        },
  
        /* — Social & brand presence — */
        "sameAs": [
          "https://www.instagram.com/sowklb"
        ],
        "logo": {
          "@type": "ImageObject",
          "url": "https://www.sowk.org/images/sowk_logo-min.png",
          "width": 180,
          "height": 180
        }
      }
    ]
  }
  </script>
  <!-- ─── end JSON-LD ─── -->
  
  <!-- Swipable Hero Image Slider (fills entire viewport below header) -->
  <section class="hero-swiper" aria-label="Highlights of SOWK Guesthouse">
    <div class="swiper">
      <div class="swiper-wrapper">
  
        <!-- Slide 1 – kitchen -->
        <div class="swiper-slide">
          <Image
            src={slideKitchen}
            alt="Open-plan kitchen with vaulted stone arches, warm pendant lighting and wooden island at SOWK Guesthouse"
            widths={[640, 960, 1440]}
            formats={['avif', 'webp', 'jpeg']}
            loading="eager"
          />
        </div>
  
        <!-- Slide 2 – twilight pool -->
        <div class="swiper-slide">
          <Image
            src={slidePool}
            alt="Twilight view of SOWK Guesthouse’s private pool bordered by olive trees and stone walls"
            widths={[640, 960, 1440]}
            formats={['avif', 'webp', 'jpeg']}
            loading="lazy"
          />
        </div>
  
        <!-- Slide 3 – floating-fruit tray -->
        <div class="swiper-slide">
          <Image
            src={slideFruits}
            alt="Breakfast tray with fresh fruits floating on the pool’s turquoise water at SOWK Guesthouse"
            widths={[640, 960, 1440]}
            formats={['avif', 'webp', 'jpeg']}
            loading="lazy"
          />
        </div>
  
      </div>
      <div class="swiper-pagination"></div>
    </div>
  </section>

  <!-- ────────────── Features Section ────────────── -->
  <section class="features">
    <h2 class="fade-title">What To Expect</h2>

    <div class="features-grid">
      <!-- Card 1 -->
      <div class="feature-card">
        <div class="icon-wrap">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none"
               stroke="#415D43" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 22c0-10 10-20 20-20 0 10-10 20-20 20z"/>
          </svg>
        </div>
        <h3>Private by Design</h3>
        <p>Your own gated villa with pool, stone garden, and campfire — completely to yourself.</p>
      </div>

      <!-- Card 2 -->
      <div class="feature-card">
        <div class="icon-wrap">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none"
               stroke="#415D43" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 22h18M4 18h16M5 14h14M6 10h12M7 6h10M8 2h8"/>
          </svg>
        </div>
        <h3>Steeped in History</h3>
        <p>Located in one of Lebanon’s oldest villages, footsteps from a Crusader-era church and stone fortress.</p>
      </div>

      <!-- Card 3 -->
      <div class="feature-card">
        <div class="icon-wrap">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none"
               stroke="#415D43" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2s4 5 4 9a4 4 0 0 1-8 0c0-4 4-9 4-9z"/>
            <path d="M8 14a4 4 0 0 1 8 0c0 4-4 8-4 8s-4-4-4-8z"/>
          </svg>
        </div>
        <h3>Soulful Comfort</h3>
        <p>Cozy interiors, handmade breakfast, and firewood blankets — designed to slow you down.</p>
      </div>
    </div>
  </section>
  
  <!-- ────────────── QUOTE STRIP (high-end version) ────────────── -->
  <section class="quote-strip" aria-label="Evening ambience at SOWK Guesthouse">
    <figure class="quote-media">
      <Image
        src={heroDusk}
        alt="Twilight view of SOWK Guesthouse framed by olive branches and lantern glow"
        widths={[640, 1024, 1600]}
        formats={['avif', 'webp', 'jpeg']}
        aspectRatio={21/9}       /* extra-wide cinematic banner */
        loading="lazy"
      />
  
      <figcaption class="quote-overlay">
        <q>Breathe deep—this is&nbsp;where time slows down.</q>
        <span class="quote-author">SOWK Guesthouse</span>
      </figcaption>
    </figure>
  </section>
  
</Layout>

<!-- ────────────── Feature-card Fade Script ────────────── -->
<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry, idx) => {
      if (entry.isIntersecting) {
        setTimeout(() => entry.target.classList.add('fade-in'), idx * 120);
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.feature-card').forEach(el => {
    el.classList.add('pre-fade');
    observer.observe(el);
  });
</script>

<!-- ────────────── Swiper (Autoplay + Fade) ────────────── -->
<link rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
<script type="module">
  import Swiper from 'https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.mjs';
  new Swiper('.swiper', {
    loop: true,
    autoplay: { delay: 3000, disableOnInteraction: false },
    pagination: { el: '.swiper-pagination', clickable: true },
    effect: 'fade',
    speed: 1000
  });
</script>

<!-- ────────────── Page-specific Styles ────────────── -->
<style>
/* ——— force Poppins everywhere ——— */
body, h1, h2, h3, p, a, button { font-family: 'Poppins', sans-serif; }

/* ---------- Hero Swiper ---------- */
.hero-swiper { height: calc(100dvh - var(--header-h)); overflow:hidden; position:relative; }
.swiper, .swiper-slide { width:100%; height:100%; }
.swiper-slide img { width:100%; height:100%; object-fit:cover; }
.swiper-slide picture{ width:100%; height:100%; object-fit:cover; }

/* ---------- CTA button ---------- */
.btn--hero{
  background:#F4B942; color:#1B2A41; font-weight:600;
  padding:.75rem 1.5rem; border-radius:8px; border:2px solid transparent;
  text-decoration:none; transition:background .25s ease,transform .25s ease;
}
.btn--hero:hover{ background:#FFF; border-color:#F4B942; transform:translateY(-3px); }

/* ---------- Features ---------- */
.features{ background:#fdfdfd; padding:5rem 1.5rem; text-align:center; }
.features h2{ font-size:2rem; color:#415D43; margin-bottom:3rem; }

.features-grid{
  display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
  gap:3rem; max-width:1100px; margin-inline:auto;
}
.feature-card{
  padding:3rem 2rem; background:#fff; border-radius:1rem;
  box-shadow:0 20px 40px rgba(0,0,0,.04);
  transition:transform .4s ease, box-shadow .4s ease;
}
.icon-wrap{ display:flex; justify-content:center; margin-bottom:1.25rem; }
.feature-card h3{ font-size:1.25rem; margin-bottom:.75rem; }
.feature-card p{ font-size:.95rem; color:#444; }

/* ---------- Fade utils ---------- */
.pre-fade{ opacity:0; transform:translateY(20px); transition:all .7s ease; }
.fade-in { opacity:1!important; transform:translateY(0)!important; }

/* ---------- Quote strip ---------- */
.quote-strip{ position:relative; width:100%; height:65vh; overflow:hidden; }
.quote-strip img{ width:100%; height:100%; object-fit:cover; }
.quote-overlay{
  position:absolute; inset:0;
  background:linear-gradient(to bottom, rgba(0,0,0,.05), rgba(0,0,0,.4));
  display:flex; align-items:flex-end; justify-content:center; padding:4rem 1.5rem;
  font-size:1.4rem; color:#fff; text-align:center; max-width:700px; margin:auto;
  font-weight:400; line-height:1.5;
}
.quote-overlay span{ font-weight:600; }

.swiper-pagination {
  display: none !important;
}

/* —— Luxe banner —— */
.quote-strip          { position:relative; height:60vh; overflow:hidden; }
.quote-media picture  { width:100%; height:100%; display:block; }
.quote-media img      { width:100%; height:100%; object-fit:cover; object-position:center; }

/* —— Overlay —— */
.quote-overlay{
  position:absolute; inset:0;
  display:flex; flex-direction:column; justify-content:flex-end; align-items:center;
  padding:4rem 1.5rem;
  background:linear-gradient(to bottom,rgba(0,0,0,0.05),rgba(0,0,0,0.55));
  text-align:center; color:#fff;
  font-family:'Playfair Display', serif;
}

.quote-overlay q{
  font-size:clamp(1.25rem,2.5vw+0.5rem,2rem);
  font-weight:500;
  line-height:1.4;
  max-width:800px;
}

.quote-author{
  margin-top:0.75rem;
  letter-spacing:0.05em;
  font-size:0.9rem;
  opacity:0.9;
  text-transform:uppercase;
  font-family:'DM Sans', sans-serif;
}

:root { --header-h: 100px; }   /* e.g. 100 px desktop & mobile */

/* ---------- Small tweaks ---------- */
.feature-card:hover .icon-wrap{
  transform:scale(1.06); filter:drop-shadow(0 3px 5px rgba(0,0,0,.05));
}
</style>
