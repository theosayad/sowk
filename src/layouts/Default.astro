---
/* Default site-wide layout */
const BASE = import.meta.env.BASE_URL;   // "/" in dev, "/sowk/" in prod
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- favicon, manifest, etc. -->
    <link rel="icon" href={`${BASE}images/favicon.svg`} />
    <!-- if you have more icons, use the same BASE prefix -->
    <!-- <link rel="apple-touch-icon" sizes="180x180" href={`${BASE}images/apple-touch-icon.png`} /> -->

    <title><slot name="title" /> | SOWK</title>

    <!-- site-wide styles -->
    <link rel="stylesheet" href={`${BASE}styles/global.css`} />
  </head>

  <body>
    <!-- page content -->
    <slot />

    <!-- optional footer -->
  </body>
</html>
